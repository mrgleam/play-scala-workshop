package exercises

/**
 * A method that returns its input value.
 *
 * def identity[A](x: A): A = x // see
 * */

/**
 * def groupBy[K](f: A => K): immutable.Map[K, Array[A]] = ???
 */

/**
 * def mapValues[W](f: V => W): MapView[K, W]
 */

/**
 * Splits this string use method split
 */

class WordCount {
  def count(text: String): Map[String, Int] = ???
}
